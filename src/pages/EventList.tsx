import React from 'react';
import { Text, View } from 'react-native';

export default function EventList() {
    return (
        <View>
            <Text>EventList 페이지</Text>
        </View>
    );
}
